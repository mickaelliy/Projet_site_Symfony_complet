{# app/Resources/views/layout.html.twig #}

<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" >
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>{% block title %} Raaaaats{% endblock %}</title>
    <meta name="keywords" content="{% block motsCles %} {% endblock %}" />
    <meta name="description" content="{% block description %} {% endblock %}" />

    {% stylesheets 'css/style.css' %}
     {# <link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}" /> #}
      <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
      <link rel="stylesheet" href="{{ asset_url }}" type="text/css" />
      {# <link rel="stylesheet" href="css/style.css"/> #}
      <link href="https://fonts.googleapis.com/css?family=Amatic+SC:400,700|Special+Elite|Dosis" rel="stylesheet">
      {# <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet"> #}
      {# jquery pour le systeme de filtre PRIX #}
      <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.3/css/all.css" integrity="sha384-UHRtZLI+pbxtHCWp1t77Bi1L4ZtiqrqD80Kn4Z8NTSRyMA2Fd33n5dQ8lWUE00s/" crossorigin="anonymous">
      <link rel="stylesheet" href="//code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
    {% endstylesheets %}
    <link rel="icon" type="image/x-icon" href="{{ asset('ratioactive-icon.png') }}" />

    
  </head>

  <body>

    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="navbar-inner">
            <div class="container-fluid">
                {# <button class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse" type="button">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button> #}
                
                <div navbar-header>
                    {{ render(controller('MickwebEcommerceBundle:Product:recherche')) }}  
                </div>
            </div>
        </div>
    </div>

    <div id="header" class="jumbotron text-center">
        <div class="logoratio">
            <a href="{{ path('mickweb_ecommerce_homepage') }}"> 
                <img src="{{ asset('ratioactive-icon.png') }}" alt="logos raaaaats" width="60" height="60"></img>
            </a>
        </div>    
        <h1> Raaaaats </h1>
        <p>
            Retrouvez toute la gamme de produits raaaaats... des produits de qualité faits par des passionnés.
            Vous aimez les rats mignons et le metal brutal, welcome to hell !
        </p>
    </div>

    {# ****************************************** #}

    {# Menu haut --- en cours de construction #}

    <div id="menu_haut">
      <ul id="onglets">
        <li class="active"><a href="{{ path('mickweb_ecommerce_homepage') }}"> Accueil </a></li>
        <li><a href="Forums.html"> Homme </a></li>
        <li><a href="Livre_or.html"> Femme </a></li>
        <li><a href="{{ path('fos_user_registration_register') }}"> S'inscrire </a></li>
        <li><a href="{{ path('fos_user_security_login') }}"> Se connecter </a></li>
      </ul>
    </div>

    <nav class="navbar navbar-inverse">
        <div class="container-fluid">
            <div class="navbar-header">
            <a class="navbar-brand" href="#">WebSiteName</a>
            </div>
            <ul class="nav navbar-nav">
                <li class="active"><a href="#">Home</a></li>
                <li><a href="#">Page 1</a></li>
                <li><a href="#">Page 2</a></li>
                <li><a href=""><i class="fas fa-shopping-basket" style="color:white" title="panier"></i></a></li>

            </ul>
            {# <ul class="nav navbar-nav navbar-right">
                <li><a href="#"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
                <li><a href="#"><span class="glyphicon glyphicon-log-in"></span> Login</a></li> #}

            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <ul class="nav navbar-nav navbar-right">
                    <li>Connecté en tant que {{ app.user.username }}</li>
                    <li><a href="{{ path('fos_user_security_logout') }}"><i class="fas fa-power-off"></i></span> Déconnexion</a></li>
                    <li><a href="{{ path('fos_user_profile_show') }}"><span class="glyphicon glyphicon-log-in"></span> Moncompte</a></li>
                </ul>
              {% else %}
              <ul class="nav navbar-nav navbar-right">
                    <li><a href="{{ path('fos_user_security_login') }}"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
                    <li><a href="{{ path('fos_user_registration_register') }}"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
                </ul>
              {% endif %}
           
        </div>
    </nav> 
    {# ****************************************** #}

    <div id="mySidenav" class="sidenav">
        <a href="javascript:void(0)" class="closebtn" onclick="closeNav()">&times;Menu</a>
        <a href="#">About</a>
        <a href="#">Services</a>
        <a href="#">Clients</a>
        <a href="#">Contact</a>
        </div>

        <div id="main">
        <span style="font-size:30px;cursor:pointer" onclick="openNav()">&#9776; Menu</span>
    </div>


    {#Ce bloc permet d'afficher le bouton connexion, deconnexion#}
    {# <div class="container">
        <div class="row">
            <div class="col-md-3">
              {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                Connecté en tant que {{ app.user.username }}
                -
                <a class="btn btn-danger btn-lg" href="{{ path('fos_user_security_logout') }}">Déconnexion</a>
                <a class="btn btn-primary btn-lg" href="{{ path('fos_user_profile_show') }}">Mon compte</a>
              {% else %}
                <a class="btn btn-success btn-lg" href="{{ path('fos_user_security_login') }}">Connexion</a>
                <a class="btn btn-success btn-lg" href="{{ path('fos_user_registration_register') }}">Inscription</a>
              {% endif %}
            </div>
        </div>
    </div> #}

    <div class="container">
        <div class="row">
            {# <div id="menu" class="col-md-3 menu">
                {#TEST INCLUDE barre de navigation#}
                {# include '::modulesUsed/navigation.html.twig' %}
                {#TEST menu panier#}
                {# render(controller('MickwebEcommerceBundle:panier:menu')) }}

                <h3>Admin</h3>
                  <ul class="nav nav-pills nav-stacked">
                    <li><a href="{{ path('mickweb_ecommerce_homepage') }}">Accueil</a></li>
                    {# Cette condition permet d'afficher "ajouter un produit" seulement aux ADMIN  #}
                  {# if is_granted('ROLE_ADMIN') %}
                    <li><a href="{{ path('mickweb_ecommerce_ajout') }}">Ajouter un produit</a></li>
                  {% endif %}
                  </ul>

                <h4>Derniers produits</h4>
               {# render(controller("MickwebEcommerceBundle:Product:menu", {'limit': 3})) #}

            {# </div> #}

            <div id="content" class="col-md-12">
                {% block body %}
                {% endblock %}
            </div>
        </div>
    </div>

        <hr/>

    <div class="reassurance">  
        <div class="picto_livr">
            <i class="fas fa-shipping-fast"></i> 
            <p>Livraison Collisimo gratuite</p>
        </div>
        <div class="picto_paiement">
            <i class="fas fa-lock"></i>
            <p>Paiement sécurisé</p>
        </div>
        <div class="picto_satisfaction">
            <i class="fas fa-thumbs-up"></i>
            <p>Satisfait ou remboursé</p>
        </div>
        <div class="picto_appel">
            <i class="fas fa-phone"></i>
            <p>Besoin d'aide<br>01 34 56 78 90</p>
        </div>
    </div>  

	<footer id="footer" class="vspace20">
		<div class="container">
			<div class="row">
				<div class="col-md-4 text-center">
					<h4>Informations</h4>
					<ul class="nav nav-stacked">
                        {{ render(controller("MickwebPagesBundle:Pages:menu")) }}
					</ul>
				</div>

				<div class="col-md-4 text-center">
					<h4>Notre entrepôt</h4>
					<p><i class="icon-map-marker"></i>&nbsp;{{ adresse }}</p>
				</div>

				<div class="col-md-4 text-center">
					<h4>Nous contacter</h4>
					<p><i class="icon-phone"></i>&nbsp;{{ telephone }}</p>
					<p><i class="icon-print"></i>&nbsp;{{ fax }}</p>
				</div>
			</div>

			<div class="row">
				<div class="col-md-12">
                    <p class="text-center">&copy;Créateur du site {{ webmaster }} {{ 'now'|date('Y') }} </p>
				</div>
			</div>
		</div>
	</footer>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

    {% javascripts 'js/main.js' 'js/slick.min.js' %}
        <script type="text/javascript" src="{{ asset_url }}"></script>
        <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
        <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
        <script src="../js/jquery.min.js"></script>
        <script src="../js/bootstrap.js"></script>
        <script src="https://unpkg.com/isotope-layout@3/dist/isotope.pkgd.js"></script>
        {# jquery pour le systeme de filtre PRIX #}
        <script src="https://code.jquery.com/jquery-1.12.4.js"></script>
        <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
    {% endjavascripts %}
    {# <script src="js/script.js"></script>
    <script src="js/jquery.js"></script>
    <script src="js/function.js"></script> #}

    <script>
            $(document).ready(function () {
                /* à la sélection du classement on met à jour */
                $(document).on("change", "#select-order", function () {
                    updateOrder();
                });
                /* au chargement de la page on lance le classement */
                updateOrder();
            });

            function updateOrder() {
                var order = $("#select-order").val();
                if (order == 'position' || order == 'prix' || order == 'name') {
                    var divList = $(".card");
                    if (order == 'position') { //tri numerique croissant
                        divList.sort(function (a, b) {
                            return  $(a).data(order) - $(b).data(order);
                        });
                    } else if (order == 'prix') {//tri numerique decroissant
                        divList.sort(function (a, b) {
                            return $(b).data(order) - $(a).data(order);
                        });
                    } else if (order == 'name') { //tri alphabetique
                        divList.sort(function (a, b) {
                            var compA = $(a).data(order).toUpperCase();
                            var compB = $(b).data(order).toUpperCase();
                            return (compA < compB) ? -1 : (compA > compB) ? 1 : 0;
                        });
                    }
                    $(".card-container").html(divList);
                }
            }
    </script>
    {# jquery pour le systeme de filtre PRIX #}
    <script>
        $( function() {
            $( "#slider-range" ).slider({
            range: true,
            min: 0,
            max: 200,
            values: [ 50, 100 ],
            slide: function( event, ui ) {
                $( "#amount" ).val( "€" + ui.values[ 0 ] + " - €" + ui.values[ 1 ] );
            }
            });
            $( "#amount" ).val( "€" + $( "#slider-range" ).slider( "values", 0 ) +
            " - €" + $( "#slider-range" ).slider( "values", 1 ) );
        } );
    </script>

    <!------- Script permettant de faire fonctionner le slider du bloc "Tendandes" -------->
    {# <script src="https://code.jquery.com/jquery-2.2.0.min.js" type="text/javascript"></script> #}
    {# <script src="../js/slick.min.js" type="text/javascript" charset="utf-8"></script> #}
    <script type="text/javascript">
        $(document).on('ready', function() {
        
        $(".regular").slick({
            dots: true,
            infinite: true,
            slidesToShow: 3,
            slidesToScroll: 3
        });
        });
    </script>


  </body>

</html>
