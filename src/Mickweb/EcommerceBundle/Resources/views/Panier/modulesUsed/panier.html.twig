<div class="well">
    <ul class="nav nav-list">
        <li class="nav-header">Panier</li>
        <li>
            <a href="{{ path('mickweb_ecommerce_panier') }}">
            {% if article == 0 %}
                Aucun article dans votre panier
            {% elseif article == 1 %}
                1 article dans votre panier
            {% else %}
                {{ article }} articles dans votre panier
            {% endif %}
            </a>
        </li>
    </ul>
</div>